<UserControl x:Class="WpfSignalSim.Connector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="15" d:DesignWidth="15" Height="15" Width="15">
    <Grid>
        <Ellipse x:Name="ConnectorVisual" Fill="DarkGray" Stroke="Black" StrokeThickness="1"/>
        <ToolTipService.ToolTip>
            <ToolTip Content="{Binding Type, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </ToolTipService.ToolTip>
    </Grid>
</UserControl>